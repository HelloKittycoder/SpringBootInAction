spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.jdbc-url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=

spring.datasource2.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource2.jdbc-url=jdbc:mysql://localhost:3306/test_liquibase?serverTimezone=UTC&useSSL=false&useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull
spring.datasource2.username=root
spring.datasource2.password=123456

# 如果是多数据源的话，下面这段会有点问题，没办法用
# spring.datasource.schema=classpath:sql/createTable.sql

# 说明：这里写的日志打印格式只对运行SpringBoot应用有效，对运行SpringBoot单元测试无效
# logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} %clr(%5level) [%thread] %clr(%-40.40C){cyan}\\(L:%L\\) - %msg%n

mybatis.type-handlers-package=com.kittycoder.globaltypehandler
